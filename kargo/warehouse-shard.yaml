apiVersion: kargo.akuity.io/v1alpha1
kind: Warehouse
metadata:
  name: wh-shard
  namespace: demo
spec:
  shard: shard-a
  subscriptions:
  - image:
      repoURL: nginx
      semverConstraint: ^1.27.0
